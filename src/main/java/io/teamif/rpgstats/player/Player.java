package io.teamif.rpgstats.player;

import io.teamif.rpgstats.roles.IRole;

public class Player extends IPlayer {
    public Player(String tribeType, IRole role) {
        super(tribeType, role);
    }
}
